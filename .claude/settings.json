{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // empty'); case \"$file\" in */slides/*.html) out=$(python3 ~/.claude/skills/slide-design/scripts/check_slides.py \"$file\" 2>&1) || { printf '[slide-check] デザイン不変条件違反:\\n%s\\n  ルール詳細: ~/.claude/skills/slide-design/references/html-slide-pitfalls.md\\n' \"$out\" >&2; exit 2; };; esac; exit 0"
          }
        ]
      }
    ]
  }
}
