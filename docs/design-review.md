# 設計レビュー — 学術的妥当性・論理整合性の監査

レビュー日: 2026-05-30 / 対象: docs 設計群 + src 実装 + 出力データ
方針: 設計を読み、主要主張をコード・データで実証検証。憶測は排し、検証済みと要確認を区別。

---

## エグゼクティブサマリー

研究の**骨格は健全**（積み上げ型3段階、欧州型IO×マイクロシミュレーションの日本実装）。
やり直しは不要。**脆弱性は「主張の言い過ぎ（overreach）」に集中しており、大半は reframe で修正可能**。

最優先で対処すべき3点（いずれも卒論審査で最も突かれる箇所）:

1. **因果主張の過大**（A-1）— 「因果推論で実証」は goods-vs-services 交絡で分離できない。"consistent with cost-push" へ格下げ。
2. **プラセボの主張が過大**（A-2）— 「β≈0 → 識別 supported」は成立しない（同一仕様の pooled プラセボは β=0.540 で無情報）。より適切な年次相関テストは shock-specificity を**方向的に支持**するが、A-1 の交絡で cost-push 固有とは分離できない。
3. **政策評価がトートロジー**（C-1）— 「逆進性101.6%解消」は行動反応ゼロの会計恒等式の機械的帰結（δ不変で確認）。"分配会計（incidence accounting）" へラベル替え。

単純な要修正（内部不整合・誤り）:
- エネルギーシェア **67% vs 73.8%** の混用（B-2）
- 輸入含有率 **IC>1**（経済的に不可能）（B-4）
- doc の式（IC×ΔP×M）と code（M×ΔP）の乖離（B-3）
- 未文書の RMSE 目標 10.5pp（C-2）

---

## A. 識別戦略（Phase 2 の核）— 最重要

推定式: `ΔCPI_{c,t} = β·(IC_c × P_import_t) + γ_t + δ_c + ε`、CPI中分類41×2021-24、two-way FE、entity-cluster SE。

### A-1. 因果主張が過大 [CRITICAL / reframe で対応可]

- **doc の主張**: INDEX.md「因果推論で実証」、research-design.md「コストプッシュ効果を識別」。
- **実体**: `P_import_t` は**集計**輸入物価（`cost_push_panel.py:73` `group=="total"`）。識別変動は「高IC財が高P_import年に多くインフレしたか」のみ。
- **致命的交絡（goods-vs-services）**: IC は財/サービス区分と機械的に相関（輸入の大半は財、サービスは低IC）。コロナ後の**財需要急増・サービス抑制→反発**は category-level の差別的トレンドであり、time FE でも entity FE でも吸収されない。→「高IC財が2021-24に多くインフレ」は cost-push と**整合的だが分離不可能**。
- **競争的輸入財除外は交絡を悪化させうる**: 上昇しなかった高IC財（衣料）を落とし、上昇した高IC財（エネ・食料）を残す。だがエネ・食料は需要非弾力な**必需品**で固有の供給ショックも持つ → 観測的に絡む。
- **5グループの価格差が識別に未使用**: エネ+203% vs 食料+50% という Phase 1 のリッチな変動を、回帰は単一集計 P_import に潰している。
- **推奨**: 「因果的に証明」→「コストプッシュと**整合的な**横断的関連」。識別仮定（デマンドプルのカテゴリ一律性）を明示し、goods-services 交絡を限界として記述。

### A-2. プラセボ検証は無情報かつ ill-conceived [MAJOR]

- **doc の主張**: research-design.md「2015-19 で β≈0 → ショック特有を支持」、contribution.md「プラセボ非有意（β=0.231, p=0.417）→ 識別仮定 supported」。
- **実証検証（本レビューで再推定）**:

  | 仕様 | β | SE | p | 95%CI |
  |---|---|---|---|---|
  | (iii) メイン: 競争的輸入財除外・ショック期 | 0.431 | 0.135 | 0.002 | [0.17, 0.70] |
  | (v) プラセボ: 全41・2015-19（設計表） | 0.231 | 0.284 | 0.417 | [−0.33, 0.79] |
  | **(v') プラセボ: 競争的輸入財除外・2015-19** | **0.540** | **0.387** | **0.165** | **[−0.22, 1.30]** |

- **pooled プラセボ β=0.540 は妥当な falsification ではない**:
  - (ill-conceived) 2015-19 は無ショック期では**ない**。P_import は 98.7→118.1 と **~20pp 変動**（2014-16 原油暴落・回復）。cost-push 転嫁があれば任意の価格変動期で β>0 が出るのが当然 → 非ゼロは想定内で disqualifying でない。しかも `shift_share` は符号反転する `P_import−100` を通すため pooled β の解釈が不安定。
  - (無情報) CI[−0.22, 1.30] は **0 も 0.431 も含む** → 支持も否定もしない。「非有意→ゼロ→supported」は absence of evidence の誤読。⚠ 逆に「プラセボの方が大きい→spurious」と読むのも**同じ誤り**。
- **年次相関は別の estimand であり、こちらが well-posed なテスト**（pooled β とは矛盾しない: 年次相関は**年内の生の横断面＝demean なし**、pooled β は **entity＋year FE 除去後** → 測っている量が違う）。競争的輸入財除外後:
  - ショック期 r: 2021 +0.42 / 2022 +0.59 / 2023 +0.60 / 2024 +0.42（n≈39、相関として有意水準）
  - プラセボ期 r: 2015 −0.08 / 2016 −0.35 / 2017 −0.13 / 2018 +0.09 / 2019 +0.20（多くは非有意）
  - このコントラストは shock-specificity を**方向的に支持**する。
- **推奨**: **年次相関コントラストを主たる falsification 証拠に据える**（pooled プラセボ β は競合結果として提示しない）。「supported」の根拠を year-by-year に置き換え、ただし **A-1 の goods-services 交絡は year-by-year にも効く**ため「cost-push **固有**とは断定不可、整合的どまり」と添える。pooled プラセボを論文で使うなら within/between 分解を別途要する（本レビューでは未実施）。

### A-3. Shift-share 推論（SE）の few-shocks 問題 [MAJOR]

- 文献に Adao-Kolesár-Morales (2019)・Borusyak-Hull-Jaravel (2022) を挙げるが、その exposure-robust SE を**実装していない**（entity-cluster SE のみ、`cost_push_panel.py:242`）。
- 単一集計 shifter（P_import_total）× IC の設計は実質「ショック1本」。time FE 下で識別変動は IC×（ほぼ単調な時間トレンド）。**n=164 は独立情報を過大表示**、p=0.002 は楽観的な可能性。
- FD 2021→22（comp-excl）β=0.371, p<0.001（n=39）は相対的に clean だが**単一エピソード依存**。
- **推奨**: BHJ exposure-robust か AKM の SE を実装。最低限「単一集計ショックゆえ有効自由度は小」と限界明記。

### A-4. 改善案（建設的・貢献を増やす）: group-specific shift-share

Phase 1 が5グループ別価格を持つのに識別は集計に潰している。
IC を 5グループ別 `IC_{c,g}` に分解し `ΔCPI_{c,t}=Σ_g β_g(IC_{c,g}×P_{g,t})+FE` とすれば、(a) 独立 shifter が複数になり識別変動・BHJ 推論が意味を持つ、(b) **グループ別転嫁の異質性**が出て論文の貢献も増す。現設計から自然な拡張。

**実装済（実験ブランチ f9e9b21）・結論**: A-4 を実装し AKM exposure-robust SE を適用 → βの点推定は頑健（0.43・energy/food主導）だが **AKM0 で有意性崩壊（p≈0.15）**。根本原因は5グループ価格が**強く共変**（共通の円為替因子）し、実効的に独立 shifter が1本しかないこと。group-specific 化だけでは救えない。

### A-5. 案2（期間延長で識別を立て直す）の検証 — **却下**（両 estimand とも期間延長は識別を救わない）[2026-05-31]

A-3/A-4 を受け「対象期間を ~2005-08 へ延ばし 2008原油・2011福島・2014-16暴落・2008-12円高 を取り込めば独立ショックが ~1→~3-4 に増える」案2を検証。**load-bearing な問い = データ可用性でなく「延長窓で識別変動が実際に増えるか」**。IO/家計/CPI の再構築なしで cgpilink1（BOJ長期連結CGPI・1960-月次・5グループ別）だけで即検証可能。再現: `tasks/_an2_shock_independence.py`。

**重要: AKM 実効ショック数 ≈ pr_group × pr_time（分離可能な共分散の下で乗法的）。2つの軸は2つの estimand に対応し、判定が分岐する。**

**軸1 — グループ間（group-specific β_g＝A-4 異質性を識別）**: 各グループ価格の年次対数変化の 5×5 共分散の participation ratio。

| 系列 / 窓 | PC1 share | pr_group |
|---|---|---|
| 円ベース 2021-24 / 2006-24 | 0.872 / 0.825 | 1.29 / **1.45** |
| 契約通貨 2007-19 / 1977-19 | 0.843 / 0.642 | 1.39 / 2.21 |

→ 5グループは強く共変（mean pairwise corr ≈0.78・共通の円為替因子＋商品スーパーサイクル）。**期間延長でも pr_group は ~1.4 止まり**。**グループ別 β_g は分離不能 → A-4 の「グループ別転嫁の異質性」という売りは識別できず、時間延長では救えない（却下）**。

**軸2 — 時間方向（pooled 単一β を IC_c × 集計P_t で識別）**: 当初、集計 shifter の年次イノベーションの逆Herfindahl（独立ショック事象数）が 1.88（2021-24）→6.35（2006-24）と増えるため「pooled β は期間延長で救える」と見立てた。だが**この pr_time は first-difference 量で、headline は levels-on-2020（shifter=IC×(P−100)＝累積）**。両者で恩恵の出方が違うため、実データで pooled β を**直接再推定**した（CPI類別を2005まで延長＝公表2020基準連結・既存パイプライン再現〔overlap 差=0〕、集計輸入価格を2005までsplice、IC 2020固定。再現: `tasks/_an2_pooled_fd.py`）。

**決定的 spec = FD-DV × Koyck-smoothed shifter S_t(δ)=Σδ^s ΔP_{t-s}・two-way FE・time(shock)-clustered**。time FE が集計インフレ（demand-pull・金融・為替）を統制する＝cost-push のクリーンな estimand。

| δ | 2016-24（現行）β / p_time | 2006-24（延長）β / p_time |
|---|---|---|
| 0.00（同時FD） | −0.05 / 0.65 | −0.02 / 0.90 |
| **0.55（プロジェクト較正値）** | **+0.10 / 0.17（null）** | **+0.07 / 0.60（null）** |
| 1.00（累積） | +0.10 / 0.32 | +0.22 / 0.05 |

→ **較正値 δ=0.55 で β は現行・延長の両窓とも非有意。現行窓では全 δ で非有意（magic δ は無い）＝distributed-lag 現象でなく level-anchoring 現象**。**期間延長は識別を救わない/強化しない**。原因は構造的: 単一の集計 shifter では time FE が時間変動（pr_time が増える部分）を丸ごと吸収し、識別は cross-sectional な IC（2020固定）だけに依存するため、年を増やしても効かない。

- 補強（consistent with・ただし弱い証拠）: levels-vs-2020 spec の β は期間延長で +0.53→−0.01 と崩壊。ただし CPI−CPI_2020 を20年に渡し取る DV は secular drift 累積・2020前後の符号領域混在という悪い対象なので主証拠にしない。
- entity-only FE（time FE 無し＝demand-pull 非統制）でのみ β≈0.31 が安定・有意だが、これは**集計インフレを統制しない交絡パラメータの精度**にすぎず、しかも延長で p は縮まらない（cur p<0.001 → ext p=0.007）。案2 の支持にはならない。
- **headline β=0.431 の頑健性そのものは案2より大きい別問題**で、既に「Phase 2a は識別限界を明記」として誠実に扱っている。本節は「headline は levels/累積では立つが、annual-difference のクリーン spec では分離識別できず、期間延長でも改善しない」と**識別限界を鋭利化する**だけで、headline を spurious と断ずるものでも robust と断ずるものでもない（過剰解釈しない）。
- **判定**: ①group-specific β_g は共線性で識別不能（軸1）、②pooled β は期間延長でクリーン識別が改善しない（軸2・実測）。**両 estimand とも案2は識別を救わない → 案2 却下**。IO表3バージョン再構築（D-3 時変IC化・×3工数）は識別を改善しないため着手しない。
- **データ可用性の所見（記録）**: 輸入価格は契約通貨/円ベース両方が1975-月次・類別で取得済（cgpilink1）。CPI類別は2020基準連結で2005まで取得・既存と完全一致（41 mid-category 全て到達）。家計五分位（e-Stat `0002070005`）は2000年〜。IO表2005/2011/2015 は公表。**いずれも取得可能だが識別が改善しないため案2には使わない**。
- **pivot 先**: 合意方針通り重心を Phase 2b（Q1-Q5 +1.42pp逆進性＝実績CPI×実シェアの恒等式・β非依存）と Phase 1（~35兆＝会計）に置く。Phase 2a は「cost-pushと整合的な点推定だが、集計インフレを統制したクリーン spec では分離識別できず期間延長でも救えない」と識別限界を明記。次の主戦場は ③ Phase 3 cost-push 再較正（C-4）。「レベル下げ」でなく識別限界をデータで定量化する誠実な構成。

---

## B. 交易損失・所得流出（Phase 1）

実際の算出式（`trade_loss.py:173-177`、IC は不使用）:
- UB: `M_g(2020) × (P_import_g − 100)/100`
- NET: `M_g(2020) × (P_import_g − P_domestic_total)/100`

### B-1. 「所得流出」概念の取り違え [MAJOR / reframe で対応可]

- NET は**相対価格スプレッド指標**であって所得フローではない。
- 証拠: 化学グループ NET が 2024 −341億 / 2025 −1505億 / 2026 −1704億と**負**（=所得が日本へ流入?）。フローなら符号反転は不自然。
- 「内閣府 交易利得・損失と comparable」（`trade_loss.py`）は、輸出側利得を引かず・5品目限定・国内CGPI使用の点で定義が異なる（内閣府2022≈−12〜16兆 vs 本推計34.6兆）。
- **推奨**: 「所得流出額」→「**輸入コスト負担**（の粗推計）」。交易損失（SNA）との概念差を注記。

### B-2. 数値の内部不整合 [要修正・単純ミス]

- 「34.6兆円（エネルギー67%）」: **34.6兆は NET 総額、67%は UB（40.7兆）のエネルギーシェア**。NET のエネルギーシェアは実データで **73.8%**（25,493/34,558）。
- 正: 「34.6兆（エネルギー73.8%）」**または**「40.7兆（エネルギー67%）」に統一。CLAUDE.md・progress.md・feasibility.md 全てに波及。

### B-3. doc と code の式の乖離 + 先行研究照合 [要修正]

- analysis-plan.md:44「IC×輸入価格変化率×輸入額」だが実コードは `M×ΔP`（IC なし）。
  - ※直接輸入財に IC を掛けるのは二重割引で不適切 → **コードの方が合理的**。doc を実態に合わせる。
- ~~齊藤2022（GNI比4.6%≈25.8兆）との照合~~ → **DEC-019 で撤回**: 「GNI比4.6%」は一次ソース（JCERコラム）に存在しない出典不明数値と判明。照合先は内閣府 SNA 交易利得・損失（2022年度 −16.4兆円・実質・2015暦年連鎖価格）に差し替え。本推計 中心35兆はグロス積上げ（輸出利得未控除・5品目限定・2015-19平時基準）ゆえ SNA のネット概念より大きく、「概念差を踏まえ同一オーダー」として提示。

### B-4. 輸入含有率 IC>1 [MAJOR / 要修正]

- `sector_import_content.csv`: sector 61=**1.070** / 231=1.018 / 271=**1.100**。含有率（シェア）が1超は経済的に不可能。
- 原因候補: `(I−A)^{-1}` を輸入比率ベクトルに適用（`io_utils.py`）。日本の競争輸入型標準は `(I−(I−M̂)A)^{-1}`。分母 `output+imports`（輸出未控除）も一因。
- **影響**: 34.6兆には不使用だが、**Phase 2 の IC_c（β回帰の右辺）と Phase 3 の content に波及**。グループ平均（max 0.64）では隠れるが、審査でセクター値を見られると含有率構築全体が疑われる。要再構成 or 検証。

---

## C. シミュレーション・政策評価（Phase 3）

> **【解決: DEC-014・2026-05-31】C-1〜C-4 は Phase 3 の「定性・例示への降格」で一括対応。**
> - **G-3（cache に δ 未適用）**: 既に解消済（io cache を δ=0.55 で force 再生成→現 cache と完全一致 diff=0・koyck≠empirical 確認）。本文の G-3 記述は stale。
> - **C-4**: β=1 Leontief を構造的上限、×0.431 を例示的スケーリングに降格（構造パラメータと称さない）。なお coherent δ=0.55 では cost-push 平均 5.2pp が実績CPI総合 ~2.5% を **~2倍 overshoot**（Leontief の中間投入経由の過剰伝播）＝β=0.431 でも C-4 較正不全は残るため、**pp 絶対値は較正済推定でなく例示**とする。
> - **C-1**: 政策削減率（エネ50%=44.7%・複合=57.7%、2022／B-4 IC是正で旧 ~101.6%/123.8% から低下）は incidence accounting（事前会計・行動反応なし）として提示。β はキャンセルする（β-invariant・実測確認）が率は IC/Leontief 構築依存で overshoot により上方バイアス＝**β-invariant ≠ robust**。特定%を結論にしない。
> - **C-2**: RMSE 9.744 は「検証達成」でなく「誤差=demand-pull 残差の分解」。**C-3**: δ=0.55 は calibration（半減期≈10.4mo・RMSE 平坦・厳密最小≈0.60）。
> - **結論に残す（真に頑健・符号レベル）**: Q1>Q5（全ショック年）・energy が最大寄与・補助は方向として gap 縮小・δ感度で方向不変。再現: `tasks/_phase3_recalib_check.py`。
> - **【暦年再生成 DEC-023・2026-07-07】** 本レビュー記録内の数値（9.744・44.7%/57.7% 等）は当時の年度ベース観測。CPI集計を暦年へ統一した現行の正準値は **Koyck RMSE 9.24pp・エネ補助 44.8%・複合 57.7%・Phase3 gap 0.32/0.92/1.08/1.17pp（2021-24）**（→ `progress.md`／decision-log DEC-023）。本記録は歴史として非改変。撤回値（101.6%/123.8%・0.10/0.32/0.38/0.41）はそのまま撤回済。


### C-1. 政策評価のトートロジー [CRITICAL / reframe 必須]

- 「エネ補助50%が逆進性101.6%解消」「複合123.8%」は、**行動反応（需要弾力性・MPC・一般均衡・転嫁）ゼロの会計恒等式の機械的帰結**。
- 証拠: δ感度で解消率 **99.5〜99.6%（δ=0.4〜1.0 で不変）**。動学を変えても不変 = 「エネルギーが格差の支配的決定因」という会計構造のみに依存。100%超は補助で符号逆転するだけ（支出シェア差×補助規模の算術）。
- 「エネが逆進性を駆動 → エネ補助で解消」は**循環**。因果的政策効果ではない。
- **推奨**: 「政策評価」→「**ceteris paribus 下の分配会計（incidence accounting）**」。マイクロシミュレーションの帰着計算自体は正当だが「行動反応を含まない事前会計」と明記。実際の補助は消費量変化・財源還流・転嫁で結果が変わると限界に。

### C-2. RMSE「検証達成」の不整合 [MAJOR]

- 9.744pp は **Koyck モデル出力（cost-push のみ）vs 実績CPI（cost-push+demand-pull）** の誤差。構造的に過小予測になるのは当然で、これを「目標<10.5pp 達成✅」と検証成功にするのは不整合（自己矛盾: analysis-plan.md:131 は同じ過小推定を「デマンドプル起因でモデル欠陥でない」と認めている）。
- 10.5pp 目標は decision-log に根拠記録**なし**（事後設定/HARKing 疑念）。9.744 vs 10.5 は薄い。
- むしろ「**誤差 = デマンドプル残差で時間と共に拡大**（2024 ~−6.8pp）」は findings として価値。validation でなく **decomposition** として提示すべき。
- 補足: 生成コードは `notebooks/04_phase3_simulation.ipynb:124-134` にある（Phase3 コード監査の「コード無し」指摘は誤り。.py のみ読んだため）。

### C-3. δ=0.55「RMSE最小化推定値」の過大表現 [MINOR]

- 独立 grid（本レビュー）: RMSE_shock 最小は **δ=0.60（9.562）**、0.55 は 9.576。曲面は [0.50,0.70] で平坦（9.56–9.58）→ **δ は弱識別**。
  - ※再現値 ≈9.58（δ=0.55）と論文 headline **9.744** の ~0.17pp 差は sample/merge 差と思われる（深追いせず）。
- 「推定値（estimated）」は標準誤差を要する確定推定の外観。実体は **calibration**。「半減期≈10.4ヶ月の calibration、RMSE は δ に鈍感」と正直に。厳密な最小は0.55でなく≈0.60。

### C-4. β=0.431 で Leontief 出力をスケール [MAJOR / 説明必須]

- β は**消費者価格**（CPI中分類）の reduced-form 回帰係数。Leontief `p=L^T m` は**生産者価格**の構造乗数。異なる変数空間の係数を構造出力に乗じている（`io_price_model.py:168,370`）。
- マークアップ・流通マージン・税込みの混合概念を生産者価格ベクトルに直掛けする単位的根拠は弱い。
- **推奨**: 「完全転嫁（β=1）モデルを実績に縮める**経験的スケーリング係数**」と明示（構造パラメータと称さない）。β=1版を併記済みなので、それを正当化に使う。

---

## D. 代表性・その他

- **D-1 [→ G-2 に統合・前提訂正] 家計調査 universe**: 当初「勤労者世帯限定 → 高齢欠落」と書いたが、**コードは実際には二人以上の世帯（高齢含む）を使用**（G-2）。真の問題は「doc/コードの universe 不一致＋潜在バグ」。代表性自体は doc が称するより広く妥当。詳細は G-2。
- **D-2 [MINOR] 必需品/裁量品分類の軽度循環**: 分類（低所得シェア>高所得シェア=必需品）を帰着計算と同じ支出シェアから作る → 軽度の循環。一文で断り。
- **D-3 [MINOR] IC 時間固定**: IO2020 の IC を 2015-24 に適用 → 10年間の輸入含有率ドリフトを無視。限界に一言。
- **D-4 [要確認] 集約・マッピング**（Phase3 監査由来、未独立検証）:
  - 41中分類→10大分類を**単純平均**集約（消費ウェイト無視、`quintile_impact.py:469`）。Phase2b（+1.42pp）と Phase3（+0.32pp）の格差差の一部はこの集約差由来かも。
  - comms と transport が同一HHコード145（通信は2022政府値下げ影響）→ コストプッシュ識別を歪めうる。

---

## E. 何を defend すべきか（強み — 捨てない）

- **積み上げ型3段階**（各Phaseが独立貢献）はリスク分散として健全。Phase1が残れば価値が残る設計。
- **競争的輸入財除外**には実データ機構（競争的アジア供給 81-84% で安定）があり、post-hoc でなく**事前定義**として説明可能。
- **FD 2021→22（comp-excl）β=0.371, p<0.001** は単一だが genuine なエピソード。
- **真摯なデータ整備**（IO×CGPI×CPI×家計調査×貿易統計の接合）。学部卒論として明らかに上位。
- 欧州型 IO×マイクロシミュレーションの**日本実装**という貢献は維持できる（政策評価を incidence accounting と正しくラベルすれば）。

---

## F. 優先修正リスト

| # | 区分 | 内容 | 節 |
|---|---|---|---|
| 1 | 必須・reframe | 因果言明を格下げ + goods-services 交絡を限界明記 | A-1 |
| 2 | 必須・reframe | プラセボを同一仕様で報告、「supported」撤回、年次相関を主証拠に | A-2 |
| 3 | 必須・reframe | 政策評価を「ceteris paribus 分配会計」にラベル替え | C-1 |
| 4 | 要修正 | エネルギーシェア 67%↔73.8% を統一 | B-2 |
| 5 | 要修正 | IC>1 の含有率式を再構成 or 検証 | B-4 |
| 6 | 要修正・reframe | 所得流出→輸入コスト負担に reframe | B-1 |
| 7 | 要修正 | RMSE を validation でなく decomposition に / 10.5pp 目標を文書化 or 撤回 | C-2 |
| 8 | 推奨・貢献増 | group-specific shift-share で識別強化 | A-4 |
| 9 | 説明 | β スケーリングの位置づけ明示 / δ を calibration と明示 | C-3,C-4 |
| 10 | ✅ 解決 | 家計調査 universe を二人以上に確定・cat02 明示フィルタ・doc 統一（DEC-011、数値不変） | G-2 |
| 11 | ✅ 解決 | 交易損失を中心推計C（2015-19基準）に統一・headline ~35兆（上限40.7）（DEC-012） | G-1 |
| 12 | 要修正 | δ感度を main baseline と一致させる | G-3 |
| 13 | 要確認 | エネルギー +213〜286% の出所を示すか +203%/+268% に置換 | G-4 |

---

## G. 数値の出所監査（別系統混用チェック）

エネルギーシェア（B-2）と**同型**の「数字を別系統の推計/系列/universe から流用」エラーを、全 docs・slides・CLAUDE.md の数値主張をコード出力CSVと突合して洗い出した。

### G-1. 交易損失 NET/UB の系統的混用 [✅ 解決済み 2026-05-30 / DEC-012]

> **解決**: NET/UB の交叉を、**新・中心推計C（2015-19 正常水準基準）**の単一手法で解消（DEC-012）。headline = **中心 ≈35兆円（2022・エネルギー67%・GDP6.3%、全て同一基準）／上限 40.7兆円（2020基準）**。2020がCOVIDトラフのため基準を2015-19平均（プラセボ期と同一窓・サイクル中立）に補正。`trade_loss.py` に3系列（C/A/B）＋ `compute_baseline_sensitivity()`（単年30.6〜42.7 vs 複数年平均~34-35で頑健性を証明）。docs を中心~35兆に統一（seminar1 発表済スライドは提示記録として 34.6 凍結, 35兆は seminar2 以降）。残課題: Laspeyres→Paasche（要数量データ）, エネルギー価格上昇率の表記ゆれ（G-4）。以下は検出時の記録。

ground truth（`trade_loss_total.csv` 2022）: **NET 総額 34,557.72（34.6兆）** / **UB 総額 40,745.32（40.7兆）** / NET エネルギー 25,493（NET の73.8%）/ UB エネルギー 27,509（UB の67.5%）。

| doc | 総額 | シェア | 整合 |
|---|---|---|---|
| CLAUDE.md / feasibility.md / slides | 34.6兆（**NET**） | エネルギー67%（**UB**由来） | ❌ 混用 |
| slides | 34.6兆（NET）+ 約6% GDP（NET整合: 34.6/561≈6.2%） | + 67%（UB） | ❌ 67% が異物 |
| progress.md | 40.7兆（UB）/ 27.5兆エネ（UB）/ 33.8兆 2023（UB） | 67%（UB） | ✅ 全UBで一貫 |

- **2023 も混線**: 33.8兆 = UB 2023（NET は 25.6兆）。34.6兆（NET 2022）の隣に 33.8兆（UB 2023）が並ぶ箇所がある → 年をまたいで NET↔UB が切替。
- **修正方針**: headline を NET か UB か**決める**。NET なら 34.6 / 2023=25.6 / シェア73.8%。UB なら 40.7 / 2023=33.8 / シェア67% / エネ27.5。現状は部品が交叉配線。

### G-2. 家計調査 universe: doc「勤労者世帯」⇄ コード「二人以上の世帯」[✅ 解決済み 2026-05-30 / DEC-011]

> **解決**: 母集団を**二人以上の世帯**（cat02=3）に確定（DEC-011）。`load_expenditure_shares` に `household_type` 引数（既定3）を追加し cat02 を明示フィルタ → **行順依存バグを除去、負担数値は不変**（gap 2022=1.4169/2023=0.8999/2024=1.1288 を再生成で確認）。doc の世帯区分表記とシェア（食料 Q1=30.6/Q5=22.3・光熱 Q1=10.2/Q5=5.7）を二人以上に統一。勤労ターゲット政策は「資格マスク（cat02=4/cat02=3）＋所得チャネル追加」を要件として別途設計。以下は検出時の記録。

raw（`household_quintile_2015_2024.csv`）には**2 universe**: cat02=3「二人以上の世帯」/ cat02=4「二人以上のうち勤労者世帯」。

- **doc の主張**: analysis-plan.md:159 / progress.md「**勤労者世帯**五分位を使用」。引用シェア food Q1=25.5/Q5=21.2・光熱 Q1=8.5/Q5=5.4。
- **検証（2019, 本レビュー再現）**:
  - 勤労者世帯(cat02=4): food Q1=**26.3**/Q5=21.6・光熱 Q1=**8.6**/Q5=5.3 → **doc のシェアと一致**（docs は勤労者世帯から引いている）。
  - 二人以上(cat02=3): food Q1=**30.6**/Q5=22.3・光熱 Q1=**10.2**/Q5=5.7 → doc と不一致。
- **コードの実態**: `quintile_impact.py:30-79` の `load_expenditure_shares` は **cat02 をフィルタせず**、`cat_data.iloc[0]`（:72）で**先頭行=cat02=3「二人以上の世帯」**を拾う（先頭行 cat02 を実測確認済）。
- **帰結（3層の不整合）**:
  1. doc は「勤労者世帯」と称するが、計算は**二人以上の世帯**で実行。
  2. Q1-Q5 負担の全 headline（**実効インフレ格差 1.42/0.90/1.13、コストプッシュ純分 0.32、政策 101.6%/123.8%**）は**二人以上の世帯**ベース。
  3. その「駆動要因」として引用するシェアは**勤労者世帯**の値 → 結果の universe と説明の universe が食い違う。
- **潜在バグ**: cat02 無フィルタ＋`.iloc[0]` は**ファイル行順依存**。順序が変われば universe が無言で切替わる。
- **修正方針**: cat02 を明示フィルタ。universe を**決める**（代表性は高齢世帯を含む二人以上の方が広く、むしろ妥当 → これを採るなら doc を「二人以上の世帯」に訂正し、引用シェアを 30.6/22.3/10.2/5.7 に差替え、下流の 1.42 等を**再計算**）。勤労者世帯を採るならフィルタ追加で下流再計算。いずれにせよ doc と結果の universe を一致させる。
- ※ D-1 はこの発見で前提が変わる（コードは高齢を含む二人以上を使用済 → 「高齢欠落」ではなく「doc/コードの universe 不一致」が真の問題）。

### G-3. δ感度分析が main baseline を再現しない [要修正 / 内部不整合]

- `policy_comparison_2022.csv` / `microsim_baseline_koyck.csv`（main）: 2022 baseline gap=**0.319**, gdp_avg=**4.729**。
- `sensitivity_delta.csv` δ=0.55 baseline 2022: gap=**0.274**, gdp_avg=**3.950**。
- 同一（δ=0.55・baseline・2022）のはずが**別値**。δ感度は main と**別系統の計算**で走っている（share base か Koyck 適用か正規化が違う）。
- 定性結論（Q1>Q5 が全δで成立）は保たれるが、**δ=0.55 のアンカーが headline 0.32 と一致しない**ため「感度で頑健」の主張が headline に紐付かない。

> **【真因・2026-05-31 追加調査】G-3 は単純な感度バグでなく Phase 3 全体の問題**:
> `io_price_model_output.csv` の `delta_cpi_koyck_pp` が**全ショック年(2021-2024)で `delta_cpi_empirical_pp`（瞬時値・δ=1相当）と完全一致**（実測 `np.allclose`=True）。つまり**キャッシュの main 出力は δ=0.55 の Koyck を実際には適用していない**（瞬時値のまま）。
> - 帰結: main の headline（コストプッシュ格差 0.10/0.32/0.38/0.41、政策解消 101.6%/123.8%、RMSE 9.744「δ=0.55」）は**実質 δ=1.0 相当で計算**されている。**C-2（RMSE 9.744 の出所）・C-3（δ=0.55 の根拠）と同一の根本問題**。
> - 一方 `sensitivity_delta._build_koyck_cpi` は δ=0.55 を（不完全ながら）適用するため乖離。さらに main と感度で 41→10 の単純平均集約に渡す category 構造が異なり（D-4）二重に乖離。
> - per-category 実測: 2022 電気代 main(koyck)=21.35 = empirical(瞬時) ≠ 正しい δ=0.55 full-carryover ≈13.95。

- **修正方針（G-3 + C-2 + C-3 を束ねた Phase 3 再生成タスク）**:
  1. `run_io_price_model_all_years` を監査（cache が stale か koyck 適用の bug か特定）。
  2. `io_price_model_output.csv` を δ=0.55・force=True で再生成し **koyck ≠ empirical を確認**。
  3. 下流（microsim_baseline_koyck / policy_comparison / sensitivity_delta）を全再生成し δ=0.55 で整合。
  4. main と感度の microsim 集約経路（41→10 単純平均, D-4）を統一。
  5. headline 数値（格差・政策%・RMSE）を再導出し docs 更新。**headline が変わる**ため要ユーザー承認。

### G-4. エネルギー価格上昇率 +203% vs +213〜286% [要確認 / 別基準の併存]

- ground truth（`annual_price_index.csv` / raw 月次）: エネルギー 2022 **年平均 302.99（+203%）**、**月次ピーク 368.0（+268%）**。
- CLAUDE.md / progress.md: **+203%**（年平均集計＝再現可能 ✅）。
- research-design.md / slides: **+213〜286%**（「ピーク上昇率」）→ **リポジトリのデータから再現不能**（品目別系列が無く 11 グループのみ。+286% は集計月次ピーク+268% を超える）。おそらく品目別（LNG 等）ピークだが**出所が未記録**。
- **修正方針**: 基準を1つに統一し明記。「+213〜286%」を使うなら品目別ピークの一次ソースを示す。示せないなら **+203%（年平均）/ +268%（月次ピーク）** に置換。

### G-5. 検証して **正しかった**数値（誤りなし・安心材料）

- 回帰: β=0.431/0.194/0.179/0.184/0.231・各 p 値・R²_within=0.228 すべて CSV と一致 ✅
- Q1-Q5 実効インフレ 1.42/0.90/1.13（2022-24）✅（ただし universe は G-2 の通り二人以上）
- コストプッシュ純分 0.10/0.32/0.38/0.41 ✅（`microsim_baseline_koyck`）
- 政策効果 101.6%/123.8%/22.3% ✅（`policy_comparison_2022`）
- IC 食料33%（=0.331）/光熱26%（=0.259）✅（`hh_category_import_content`、消費 universe ラベルは正しい）
- GDP 比 約6% = 34.6/561 ✅（NET 整合）

→ **転記ミスは無い**。問題は「どの系統の値か」の取り違え（G-1〜G-4）に限局。

---

## 検証メモ（再現用）

主要主張はコード出力CSV・raw データと突合して検証（検証は ad-hoc スクリプトで実施・非保持）。再現に使うソース:

- 回帰再推定・プラセボ対称化・年次相関: `src/analysis/cost_push_panel.py`（`build_panel_dataset` + `run_panel_regression`、要 `PYTHONPATH=.`）。プラセボ同一仕様 β=0.540、年次相関は `data/processed/price-indices/panel_cost_push.csv`。
- δ grid（RMSE）: `src/analysis/io_price_model.py` の `run_io_price_model_all_years(delta=...)` を δ で掃引し実績CPI（`panel_cost_push.csv` の `delta_cpi`）と比較 → 最小は δ=0.60、曲面は平坦。
- 交易損失 NET/UB/中心C・価格ピーク: `data/processed/trade-loss/`（`trade_loss_total.csv`, `annual_price_index.csv`, `trade_loss_baseline_sensitivity_2022.csv`）+ `data/raw/boj-cgpi/import_prices_extracted.csv`。
- 家計 universe: `data/raw/household-survey/household_quintile_2015_2024.csv`（cat02=3 二人以上 / cat02=4 勤労者）。
- IC>1: `data/processed/import-content/sector_import_content.csv`。
- 政策・分位: `data/processed/simulation-params/{policy_comparison_2022,sensitivity_delta,microsim_baseline_koyck}.csv`。
